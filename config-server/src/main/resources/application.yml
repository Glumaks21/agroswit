server:
  port: 8888

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/{application}

management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include: "*"
